# docker-compose for phoenix/elixir application

## env
- phoenix/elixir
- maria db
- phpmyadmin
- proxy


## usage
```
docker-compose build && docker-compose up -d
curl api.localhost:port
```
need to add api.localhost to /etc/hosts
